@model EmptyStateViewModel

<section class="section_gap">
    <div class="container">
        <div class="row">
            <div class="col-12 text-center">
                <h3 class="title_color">@Model.Title</h3>
                <p>@Model.Message</p>
                <a href="@Model.ButtonLink" class="btn theme_btn button_hover mt-3">@Model.ButtonText</a>
            </div>
        </div>
    </div>
</section>
